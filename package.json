{
  "name": "juansilva.design",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Juan Silva's bilingual design engineer portfolio.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:worker": "tsc -p functions",
    "check": "astro check && npm run check:worker && astro build",
    "deploy": "node scripts/deploy.mjs",
    "deploy:check": "node scripts/deploy.mjs --check",
    "deploy:status": "node scripts/deploy.mjs --status",
    "deploy:worker": "wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@cloudflare/workers-types": "^5.20260812.1",
    "basic-ftp": "^6.2.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.122.0"
  }
}
