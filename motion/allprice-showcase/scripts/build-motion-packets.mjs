import {readFile,writeFile} from 'node:fs/promises';
import {resolve} from 'node:path';
import {buildFramePackets} from '/home/jaypy/.agents/skills/product-launch-video/scripts/frame-packets.mjs';
const assets=JSON.parse(await readFile('capture/motion-assets.json'));
const global=`
## Binding production context (approved layout takes precedence over generic defaults)

You build exactly one frame. The current file is its approved wireframe; preserve the OUTER composition/viewport positions and exact English caption, replacing internal UI stand-ins with real captured raster pixels. Do not rebuild or retype the UI. Use the derived frame-specific assets below instead of mounting the archival files named earlier; those are provenance sources, not required DOM elements. Do not mount an asset twice under one src. No network URLs, audio, fetching, image processing or extra files. Read this whole packet, frame.md, and your approved sketch; then author only your assigned HTML with apply_patch.

Canvas 1920x1080. Root width/height 100%, absolute inset 0, overflow hidden, container-type:size. Full-duration white background clip. Main UI bottom y936 and caption top960 are USER APPROVED exceptions to generic 83% keepout. Caption is frame-owned English explanatory text, not narration: render it as approved. It stays level, x96 y960 width1728 height64, Lato 700 44px line-height1.2, centered, color#234DD7. Main09 uses30px. Final main caption y933, credit y1007 28px. Remediation credit y1010 28px color#384257. Local font @font-face entries are assets/fonts/lato-400-latin.woff2 and assets/fonts/lato-700-latin.woff2. No shadows or grading. Actual screenshot colors remain exact. Restrained visual movement, no breathing or bounce during reading. Background white is always present, no fade-to-black. Avoid independent card floating.

Bare template only. #root styles the composition root. Unique prefixed ids/classes. Embed styles/scripts inside template. GSAP is supplied by host; use ONE synchronous gsap.timeline({paused:true}) registered under exact frame id. No CSS transforms on GSAP-owned elements; no async timeline creation. Use absolute fromTo endpoints, immediateRender:false for later tweens sharing a property. No repeat/yoyo/timers. The full duration needs genuine holds/motion spanning it; a constant-valued camera hold tween is an explicitly approved hold, not a dummy timeline pad. Never add an exit to a non-final frame. Main11 and last supporting frames are final. No rendering/checking; orchestrator owns those.

Loop endpoint contracts: match all opening visible channels at local duration-1/60 (last sampled frame), retain through duration. rem01 opens with pair at approved x96/x994 y378 w830 h265.859375, rotationsY +6/-6, perspective2400px, captions/labels fully visible; rem04 must end exactly there with opening caption. responsive01 opens at desktop x96 y291 w1215 h438.75, rotationY+4; mobile x1426 y98 w390 h811, rotationY-4, perspective2400; labels/caption fully visible. responsive03 must restore this exact state, including hero rasters and opening caption. interactions01 opens both captured states closed, neutral camera, hidden cursor, approved opening caption; interactions03 must return exactly to that. Use a duplicated DOM caption element for loop return, opacity0->1; do not rewrite textContent.

Do not show claimed metrics, fabricated app workflows, toggles, signup or support navigation. Figma diagrams are editorial metadata diagrams, NOT claimed Figma screenshots. Preserve exact metadata. Numbers in source illustrations are moving context only. Keep every UI image ungraded.
`;
const main=`
## Shared main-tour structure — identical across all main frames

At normal pose the page viewport is x240 y36 width1440 height900. A perspective stage covers the root (perspective2400px). Within it place a positioner at x240/y36; within it a rotation rig (transform-origin 50% 0%); within it a scale wrapper (transform-origin 50% 0%); within it a viewport width1440 height900 overflow:hidden, border-radius12px, NO border/shadow. Put scroll track inside viewport. The cropped page raster has CSS width1440, CSS height pageHeight from JSON. Its track y is cropTop minus absolute document scrollY. A unique header raster width1440 height86 stays at viewport top, z-index3. Header is never in the scroll transform. Caption remains outside all camera wrappers. Do not add browser chrome, labels, progress bars, or extra brand art not approved. All main cuts must have zero rotation, scale1, position240/36, opacity1 and header at0/0. Every scroll segment uses power2.inOut and reaches its numerical endpoint before the cut. Holds use same from/to coordinate. End/start page pixels must coincide exactly. Main01 begins scale1.2 positioner y158 (relative offset122), giving x96 automatically through center-top scaling. Ends scale1/y36/zero rotations; viewport mask opens from bottom380px to0. Main11 opens neutral then scales to .9733333333 by1s, before final gentle pullback and white fade.

All main captions are fully visible at t0, remain level, no frame-level caption slide/fade that flashes across matched cuts. Main01 may introduce only the camera, main11 may fade whole content to white at4.75. For main04 preserve neutral endpoints: add shallow rotationY0->4->0 within .05–1.0s, not a rotated t0 (handoff wins). Other main frames neutral.

If FAQ parts are supplied: do not mount the ordinary whole page raster. Scroll track holds faqTop at docCropTop, faqTail at splitDocY-cropTop, answer at splitDocY-cropTop, openIcon at given docY-cropTop and x. Answer crop is full width1440 x81: reveal it with clip-path inset(0 0 100% 0) -> inset(0 0 0% 0). Translate faqTail +81 over the SAME tween window and ease; reverse together. openIcon opacity follows expansion. Top height=9749-cropTop; tail height=cropBottom-9749. Source question2 plus center is [1229,9713] in document. Cursor is inside viewport, authored UI coordinates, arrow tip at [1229,532] during scroll9181. Add a modest click pulse at the actual target, not a detached callout.

Feature rail: cover only the exact measured rail box, inside scroll track at y=4237.5-cropTop/x136/w1168/h638; inside it put supplied full rail width1614 height638. Horizontal x0->-446 uses same ease/timing as cursor tip travel [1030,458]->[584,458] (local viewport coordinates during scroll3927). No ghost card: this is a scroller, not drag-and-drop. Return rail x0 by4.8 before vertical departure. Hide cursor before subsequent page scroll. Whole source rail remains pixel-faithful.
`;
const supporting=`
## Supporting-film crop geometry

Remediation: original/optimized images are genuine full-composite crops, exactly1280x410. Pair frames display each830x265.859375 at x96 and994,y378 with labels at y314. Diagram frames display source810x259.453125 at x96 y367, label y299; editorial diagram at x998 y119 width826. Read approved sketch for exact row coordinates. Use Lato readable28–30px node text, bold46px count. No synthetic auto-layout labels.

Responsive: desktop label x96 y183,w1215,h42; mobile label x1426 y36,w390,h42. Desktop hero raster1440x520 scales uniformly .84375 to1215x438.75 at96/291. Mobile hero raster390x844 at1426/98 in390x811 clipped viewport. Cards/plans desktop viewport x96 y244 w1215 h665. Fit the supplied complete cropped desktop content with object-fit:contain so all three cards / both plan columns survive (never stretch pixels); white pane. A separate centered scale wrapper can present source1440x1097 or1440x1186 at up to665/sourceHeight uniform scale, source plane centered horizontally. Mobile pane x1426 y98 w390 h811 uses source390-wide at1x and moves vertically according to mobileScrollStops. Keep mobile sticky header unique supplied76px raster. Use sourceTop minus scroll stop: section crops start at0. Plan mobile scroll550 shows Mensal and1575 shows Anual; first reading surface includes real complete name and price. Do not mount menu or external-flow simulations. Closing return uses your frame-specific heroDesktop/heroMobile as extra layers, not other frames' asset URLs. Native opening pose/captions exact at5.983333333.

Interactions: mobile x104 y93 w390 h811, no device bezel; desktop viewport x622 y93 w1194 h811. Desktop source canvas width1440, scale1194/1440=.8291666666667, viewport source height=978.0904523. Keep outer scale separate from source layers. FAQ top at0 with height568, following tail at568, answer at568 with81px reveal/push, question2 icon at1211/514; header separate86px at0. Desktop question2 cursor tip in composition coordinates: x622+1229*.8291666666667=1641.0458,y93+532*.8291666666667=534.1167. Mobile hamburger tip x104+356=460,y93+38=131; close X tip x460,y207. Both the closed and open mobile rasters are real captures; for menu overlay animate the open-state surface opacity (short .2–.4s), leaving real backdrop intact. Do not fabricate DOM menu items. During FAQ films mobile remains closed; during menu film desktop remains closed. FAQ open/close uses clip reveal and +81px tail translate, not whole-frame dissolve. FAQ t0 open state for interactions03 MUST match interactions02 end. Loop closes to opening caption and hidden cursor.
`;
for(const board of ['STORYBOARD.md','../allprice-remediation.storyboard.md','../allprice-responsive.storyboard.md','../allprice-interactions.storyboard.md']){
 const packets=buildFramePackets({projectDir:process.cwd(),storyboardPath:resolve(board),extraSections:block=>{const id=block.match(/src:\s*compositions\/frames\/([^\s]+)\.html/)[1];return global+(id.startsWith('main')?main:supporting)+`\n## Your staged source assets and authored constants\n\n\`\`\`json\n${JSON.stringify(assets.frames[id],null,2)}\n\`\`\`\n`;}});
 console.log(board,packets.map(p=>`${p.frameId}:${p.bytes}`).join(' '));
}
await writeFile('.hyperframes/build-plan.json',JSON.stringify({stage:'animation-build',main:58,remediation:16,responsive:18,interactions:16,frames:Object.keys(assets.frames),fps:60,renderApproved:false},null,2)+'\n');
