---
import LegalPage from "../../components/LegalPage.astro";
import { localeStaticPaths, type Locale } from "../../i18n";

export function getStaticPaths() {
  return localeStaticPaths();
}

interface Props {
  lang: Locale;
}

const { lang } = Astro.props;
---

<LegalPage {lang} kind="cookies" />
