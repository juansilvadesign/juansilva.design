# Copy to .dev.vars for local Worker development (`wrangler dev`). Never commit
# real values. In production these are Worker secrets: `wrangler secret put NAME`.
TURNSTILE_SECRET_KEY=
RATE_LIMIT_SECRET=
GMAIL_USER=
GMAIL_PASS=
